# Test Configuration
app.name=Demo Spring Boot Application
app.version=1.0.0
app.environment=test

# Server Configuration
server.port=0

# Logging Configuration
logging.level.root=WARN
logging.level.com.example.demo=INFO
